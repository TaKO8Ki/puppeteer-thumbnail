version: '3'
services:
  web:
    build: .
    container_name: "puppeteer-thumbnail"
    environment:
      - VIRTUAL_HOST=wooooo.cf
      - PORT
    deploy:
      replicas: 2  
    working_dir: /app
    command: npm start
    volumes:
      - .:/app
    ports:
      - $PORT

networks:
  default:
    external:
      name: shared
